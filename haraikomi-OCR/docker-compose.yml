services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
      - "8080:8080"
    volumes:
      - .:/app
      - ./.cache:/app/.cache
    environment:
      - HF_HOME=/app/.cache/huggingface
